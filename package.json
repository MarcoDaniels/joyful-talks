{
  "name": "joyful-talks",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "sync": "ts-node sync/index.ts",
    "sync:build": "tsc --noEmit",
    "sync:tsc": "tsc --noEmit",
    "start": "elm-pages develop",
    "build": "elm-pages build",
    "clean": "rm -rf dist",
    "postinstall": "elm-tooling install"
  },
  "dependencies": {
    "cockpit-http-client": "0.0.1",
    "elm-pages": "1.5.4"
  },
  "devDependencies": {
    "elm-format": "0.8.4",
    "elm-tooling": "1.1.0",
    "node-sass": "5.0.0",
    "ts-node": "9.1.1",
    "typescript": "4.1.5"
  }
}
